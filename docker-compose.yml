services:
  valkey:
    image: valkey/valkey:8.1.3
    container_name: valkey
    restart: unless-stopped
    ports:
      - "6379:6379"
    command: >
      valkey-server
      --appendonly yes
      --appendfsync always
      --dir /data
